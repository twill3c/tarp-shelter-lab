# SPEC.md — tarp-shelter-lab(TARP & SHELTER LAB)

<!-- scaffold template v1.30.0 から展開(2026-09-06)。以後このファイルはプロジェクトが育てる -->

本ファイルが正本である。原案は利用者提供の仕様書
「TARP & SHELTER LAB — 米国政府・米軍サバイバル・マニュアルを参考にしたインタラクティブ・
タープ設営シミュレーター仕様書 v1.0(2026-09-06)」(以下「原案 §n」)。
原案はディスクに実体が無く添付として届いたため、`docs/original-spec-outline.md` に
節構成と本 SPEC が採った/採らなかった項目の対応表を残す。

## 1. 目的

米軍サバイバル・マニュアル(FM 21-76 系)に載るタープ設営を、
「見る → 動かす → 張る → 試す → 理解する」の順で体験できる無料 Web アプリ。
ブラウザだけで完結し、外部 API・DB・認証を使わない(原案 §1・§26)。
最初に A-Frame 一種類を触れるところまで完成させ、その後に Lean-To / Diamond、
風・雨・MISSION・ロープワークを順に足す(原案 §33・§36)。

## 2. 機能要求

| ID | 要求 | 優先度 | 段階 |
|---|---|---|---|
| F-01 | シェルター選択。MVP は A-Frame / Lean-To / Diamond の三種、Phase 5 で鋤先とティピーを追加(五種) | must | L0 データ / L1 UI / L8 拡張 |
| F-02 | SVG の設営フィールドにタープ・ポール・ロープ・ペグを描く | must | L1 |
| F-03 | ポール・ペグをトレイから目標位置へドラッグ(マウス・タッチとも Pointer Events で一本化) | must | L1 |
| F-04 | 設営ステップ表示(展開 → ポール → ペグ → ロープ → 張力 → 完成) | must | L0 状態機械 / L1 UI |
| F-05 | 配置判定: 未充填の目標との距離が許容半径以内なら充填、外れなら差し戻してミス +1 | must | L0 |
| F-06 | 完成判定: 展開済み・全ポール充填・全ペグ充填・全ロープ正しいペグに接続・全張力 ≥ 閾値・全ポール高さが範囲内 | must | L0 |
| F-07 | RESET で初期状態へ戻る | must | L0 |
| F-08 | AUTO: 正しい手順を段階アニメーションで再生する(スコア対象外) | must | L0 手順列 / L1 再生 |
| F-09 | ベストスコアを localStorage に保存し再読込後も保持 | must | L0 |
| F-10 | 完成時にスコア(0–1000)とランク(S/A/B/C/D)を表示 | must | L0 計算 / L1 表示 |
| F-11 | 張力: ロープ長と目標長の差から 0–100 を出し、たるみ・届かないを文で返す | must | L0 |
| F-12 | ポール高さ(cm)を調整でき、範囲外なら「居住空間 LOW/HIGH」を返す。居住性(座って過ごせる床の広さ)を測って出す | should | L0 / L10 |
| F-13 | 各シェルターに参考資料(資料名・版・年・出典 URL・権利状態)を紐づけて表示 | must | L0 データ / L1 表示 |
| F-14 | HINT: 次に置くべき目標を強調する(使用回数はスコア減点) | should | L1 |
| F-15 | フリート共通フッタ(MIT License / GitHub / 歩き方 / 設計図 / App Menu の 5 項目・下部固定) | must | L1 |
| F-16 | Go Functions(`/api/challenge` `/api/shelter` `/api/health`)。クライアントだけで完全動作することが前提 | could | 保留(§7) |
| F-17 | 風シミュレーション: 風向と風速を変え、安定度 STABLE/CAUTION/DANGER と矢印・揺れを出す | should | Phase 2(L3) |
| F-19 | 雨シミュレーション: 雨量を変え、排水の判定 GOOD/CAUTION/POOL と雨滴・溜まりを出す | should | Phase 2(L4) |
| F-18 | MISSION: 課題の提示・達成判定・クリア記録 | should | Phase 3(L5) |
| F-20 | クイズ: 状況から型を選ぶ 4 択。正解はモデルから導く | should | Phase 4(L6) |
| F-21 | ロープワーク: 結び方の手順と模式図、設営工程との対応 | should | Phase 4(L7) |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的配信のみで動く(ビルド成果物は HTML/CSS/JS/JSON)。サーバ側処理に依存しない | `vite build` の成果物を静的サーバから配って実ブラウザ検品(L1) |
| N-02 | 縦画面のスマートフォン幅(360px)と PC 幅(1280px)の両方で横溢れが無い | Playwright で二幅の `scrollWidth ≤ clientWidth` を測る(L1) |
| N-03 | 外部 API・外部フォント・CDN を読まない(課金経路ゼロ) | ビルド成果物に `https://` の script/link が無いことを検査(L1) |
| N-04 | 判定・張力・スコアの規則はデータと純関数だけで決まり、UI 非依存 | `tests/` が DOM 無しで走る(L0) |

## 4. 設営モデル(原案 §7・§9 の具体化)

原案 §9 は「完全な物理シミュレーションではなく教育上分かりやすい近似モデル」と定める。
本 SPEC はその近似を**値の入った規則**にする。単位は cm、座標は平面図(真上から見た図)。

### 4.1 フィールドと座標

- フィールドは `field.w × field.h`(cm)。原点は左上、x は右、y は下。
- タープは `tarp.folded`(畳んだ状態)・`tarp.flat`(展開して地面に広げた状態)・
  `tarp.pitched`(張り終えた状態の接地投影)の三つの多角形を持つ。
  展開(F-04 の第 1 段)で folded → flat、ポール充填で flat → pitched へ遷移する。
  **pitched の寸法は当該シェルターの理想ポール高さから三平方で導く**(例: 300 cm のタープを
  高さ 120 cm の A 形に張ると片側の接地幅は √(150² − 120²) = 90 cm)。
- 目標(ポール・ペグ)は `x, y` を持つ。ロープは `anchor`(タープ側の点)と `peg`(接続先ペグ ID)を持つ。
- 張り終えた形は `tarp.panels`(稜線で分けた屋根面。各面は `poly` と明度 `shade`)と
  `ridge`(稜線の折れ線)で描く。**平面図のままでは「地面に置いた布」と「張った屋根」が
  同じ絵になり、A 形に見えない**(loop_001 の目視で判明)。面の明度は基準色
  `hsl(41.5 61% 68%)` の明度に `shade` を掛けて出す。半透明を重ねるだけでは下地と同系色で差が読めない。
  `panels` の面積和は `pitched` の面積に一致しなければならない(G-01・靴紐公式で検算)。

### 4.2 配置判定(F-05)

- ドロップ位置と**未充填**の目標のうち最も近いものとの距離が `tolerance`(既定 40 cm)以内なら、
  その目標を充填する。以内でなければミス +1 とし、部品はトレイに戻る。
- 40 cm は「ペグを打ち直さずに済む」誤差として置いた設計値であり、実測値ではない。
  端末幅 360px でフィールド 800 cm を描くと 40 cm ≈ 18px になる(見込み。L1 の検品で
  指で置けることを確かめる)。

### 4.3 ロープ長・張力・たるみ(F-11・原案 §9.1–§9.3)

```
ropeLength   = distance(anchor, peg.position)
targetLength = distance(anchor, peg.target)
tension      = clamp(100 − |targetLength − ropeLength| × 2, 0, 100)
sag          = 1 − tension / 100
```

- `tension ≥ 70`(差 15 cm 以内)を「張れている」とする(G-03)。
- 差が −15 cm より短ければ「たるみが発生しています」、
  +15 cm より長ければ「ロープが届きません」。
- ペグを放したとき差が 15 cm 以内なら目標へ吸着し tension = 100 とする(ゲーム性の判断)。

### 4.4 ポール高さ(F-12・原案 §9.4)

各ポールは `height.min / ideal / max`(cm)を持つ。現在値が min 未満なら「居住空間 LOW」、
max 超なら「居住空間 HIGH」、範囲内なら OK。初期値は ideal(高さ調整は任意操作)。

### 4.5 完成(F-06)

F-06 の六条件をすべて満たしたとき `complete`。どれか一つでも欠ければ未完成(G-04)。

### 4.6 スコア(F-10・原案 §13.2)

```
timePenalty = max(0, seconds − 30) × 5
score       = clamp(1000 − timePenalty − mistakes × 60 − hints × 40, 0, 1000)
rank        = S (≥900) / A (≥750) / B (≥550) / C (≥350) / D
```

秒は最初の操作から完成までの経過。AUTO 再生は計測もスコア記録もしない。
係数は設計値(実測ではない)。MISSION 導入時に「材料使用数」「ヒント回数」の重みを見直す。

### 4.7 風(F-17・原案 §9.6 §10)

完全な空力ではなく、**向きが効くこと**が分かる近似にする。係数は設計値であり実測ではない。

- **風向**は「どこから吹くか」の方位角 `windFrom`(度)。0 = 画面の上から、90 = 右から。
  風の進む向きのベクトルは `v = (−sin θ, cos θ)`。
- **正面投影幅** `w(θ)` は、`tarp.pitched` を風に垂直な軸 `u = (cos θ, sin θ)` へ射影した幅
  (支持関数)。A-Frame の張り終えた形は 180 × 300 の矩形なので、
  θ=0 で 180、θ=90 で 300、θ=45 で (180+300)/√2 = 339.411(閉形式・T-020 の期待値)。
- **正面投影面積** `A = w(θ) × h / 2`。`h` は立っているポールの最大高さ。
  屋根は三角形の断面なので 1/2 を掛ける。
- **抗力係数** `Cd` は、開口が風上を向いているとき 2.0、それ以外 1.2。
  **片側だけを持ち上げる型では、開口は幾何から決まる** —— 持ち上げた側が開く。
  だから `opening` は自由な宣言ではない。張った形の重心からポールの重心へ向かう方位と
  45 度以内で一致しなければならない(G-31)。ポールが重心にある型
  (A-Frame の両端・ティピーの中央)はこの規則の対象外で、**対象外の理由をデータに書く**。
  この検査を入れた 2026-09-08 に、Lean-To が逆(ポールは北・開口は南と宣言)であることが分かった。
  開口の方位は `opening`(度、開口が向く方位)。`windFrom` との角度差が 90 度未満なら
  風が開口へ入る。`opening` が `null`(両側が閉じる型)なら常に 1.2。
- **荷重**(点) `load = 100 × q × Cd × A / A_ref`。`q = (風速/100)²`(動圧は速度の二乗)、
  `A_ref = 22,500 cm²`(300 × 150 / 2 = 基準の一枚屋根)。
- **保持力**(点) `hold` は全ロープの張力の平均。張れていないほど耐えられない。
  ロープが一本も接続されていなければ 0。
- **安定度** `stability = clamp(hold − load, 0, 100)`。
  `STABLE ≥ 60` / `CAUTION ≥ 30` / `DANGER < 30`。

このモデルが言えるのは向きと張力の効きだけで、実際の耐風性能ではない。

### 4.8 雨(F-19・原案 §9.3 §11)

水がどこへ流れるかではなく、**勾配と張りが排水を決めること**が分かる近似にする。

- **水平距離** `run` は、屋根面の頂点から稜線(`ridge` の両端を通る直線)までの最大距離。
- **勾配** `slope = atan(h / run)`(度)。`h` は立っているポールの最大高さ。
  実測(2026-09-07・データから計算): A-Frame は run 90 cm で 53.1301 度、
  Lean-To は 260 cm で 29.9816 度、Diamond は 212 cm で 31.5169 度(いずれも理想の高さで)。
- **最も緩い面が排水を決める**。屋根は一番寝ている所に溜まるからである。
- **排水度** `drainage = clamp(slope / 45, 0, 1) × (平均張力 / 100)`(0〜1)。
  45 度で勾配側は満点。張りがゆるむと、勾配があっても布がたわむので下がる。
- **溜まり** `poolTarget = clamp(雨量/3 − 排水度, 0, 1)`。雨量は 弱=1 / 中=2 / 強=3。
  画面の水たまりはこの値へ時定数 2 秒で近づく(見せ方であって判定ではない)。
- **判定は雨量込みで出す。** 排水度は屋根の能力(勾配 × 張り)であって、
  その雨に足りているかは別の問いである。判定は溜まりで決める:
  `poolTarget < 0.05` → 「排水 GOOD」/ `< 0.25` → 「排水 注意」/ それ以上 → 「水が溜まりそう」。
  0 でなく 0.05 を境にするのは、手で打ったペグが数 cm ずれるからである。厳密な 0 を要求すると
  **ほぼ完璧な設営でも強い雨で必ず「注意」になる**(loop_004 の実測)。
  雨量 0 のときは判定を出さない(判定する対象が無い)。
  **能力だけで「GOOD」と出すと、その隣で水が溜まって見え、画面が自分と矛盾する**
  (loop_004 の目視で実際に起きた)。排水度そのものは「排水力」として別に表示する。

**この近似の限界**: 張り終えた形(`tarp.pitched`)はデータで固定なので、ポール高さを上げても
接地幅は変わらない。実際は高くするほど接地幅が狭くなり、勾配の増え方はこのモデルより緩い。
モデルの中では「高くすれば必ず排水が良くなる」が成り立つが、**現実の設営ではそうとは限らない**。

### 4.9 MISSION(F-18・原案 §13)

課題は**判定できる述語の集まり**として書く。各課題は次を持つ。

| 欄 | 意味 |
|---|---|
| `conditions` | 課題が課す天候(`windFrom` / `windSpeed` / `rain`)。課題中は操作盤を課題が握る |
| `limits` | 使える材料の上限(`poles` / `pegs` / `ropes`)。超える型は選んでも達成できない |
| `requirements` | 完成に加えて要求すること(`minStability` / `rainVerdict` / `maxSeconds` / `maxMistakes`) |

判定は純関数で、**満たしていない項目を必ず列挙する**(根拠を言えない記号を出さない — HC-079)。
達成/未達とスコアは**別に出す**。同じ画面で二つの意味を混ぜない(HC-202)。

**課題は型を選ばせるものでなければならない**(G-22)。どの型でも達成できる課題、
どの型でも達成できない課題は何も教えない。実測(2026-09-07・完成状態で計算):

| 課題 | 条件 | 達成できる型(2026-09-07・型 5 種で再実測) |
|---|---|---|
| M-01 強い雨 | 雨量 3・排水 GOOD・120 秒以内 | A-Frame / 鋤先 / ティピー(勾配の急な三つ) |
| M-02 強い風 | 北から 70%・安定度 60 以上 | A-Frame のみ(71.8) |
| M-03 最小材料 | ポール 1 本まで | Diamond / 鋤先 / ティピー |
| M-04 軽装で風を凌ぐ | ペグ 4 本まで・**南**から 55%・安定度 60 以上 | Lean-To / 鋤先(どちらも開口が北なので、南風では風下を向く) |
| M-05 広く使える屋根 | 雨量 2・居住性 0.25 以上・排水 GOOD | Lean-To / Diamond(広さで上位の二型) |

**課題は一意である必要が無い。** 達成の道が複数あるのは課題として健全である。
要求は「達成できる型が 1 つ以上・できない型が 1 つ以上」(G-22)であって、唯一性ではない。
一方**クイズは 4 択なので正解が一意でなければならない**(G-25)。二つは別の要求である。

**原案 §13.3 の材料指定(ROPE ×2)は現在のデータでは達成できない。**
どの型も四隅に張り綱が要るので最少で 4 本である。上限を 4 本に読み替えた。

**このモデルが測る二軸(耐風・排水)では A-Frame が常に勝つ。**
Lean-To の利点(前が開いて出入りと焚き火ができる)はモデルに入っていない。
だから M-04 は材料の制約で A-Frame を外している —— **モデルの外にある長所を、
モデルの中の制約で代用している**ことを明記しておく。

**鋤先が Lean-To と Diamond を完全に上回る**(2026-09-08 実測)。勾配 42.9 対 30.0 / 31.5、
正面投影面積も小さく、材料もポール 1・ペグ 3 でいちばん少ない。
上限で鋤先だけを外すことができない(すべての材料で最少だから)ので、
**この二型はクイズで一度も正解にならない**。
それぞれの実際の利点 —— Lean-To は前が大きく開いて焚き火に向くこと、
Diamond は四方が均等で張り方が分かりやすいこと —— は**どちらもモデルの外**にある。
モデルが測るのは耐風と排水の二軸だけである。

### 4.10 クイズ(F-20・原案 §14)

「この状況ならどの型を選ぶか」の 4 択。選択肢は 3 つの型と「どれも適さない」。

- **正解は書かない。モデルから導く。** 設問が持つのは状況(`windFrom` / `windSpeed` / `rain`)、
  材料の上限、そして**基準**(`criterion`)だけである。
  基準は `weather`(その天候に耐えられるか)と `space`(座って過ごせる広さが取れるか)。
  **耐えられるかと広いかは別の問い**なので、一つの数に混ぜず設問ごとに切り替える。各型を張り終えた状態にして**適性**を計算し、最大のものを正解とする。
- **適性** `fitness = min(安定度 / 100, 1 − 溜まり)`(0〜1)。**先に壊れるほうが決める**ので
  平均ではなく最小を採る。風が無ければ安定度は保持力、雨が無ければ溜まりは 0 になる。
- 材料の上限を超える型は**適格でない**(選べない)。
- 適格な型の最大適性が `0.5` 未満なら、正解は「どれも適さない」(耐候の設問だけ。広さの適性は 0.5 を超えないので、この規則は当てはめない)。
  このとき一意性は**閾値からの隔たり**で見る。同じ `0.10` を要求する ——
  最良が閾値のすぐ下にある設問は、その型を選んだ人を責められない
  (実測: 北から 100% + 雨量 3 では最良が 0.424 で隔たり 0.076 しかなく、設問として不適格だった)。
- 回答後は、**各型の実数値を並べて理由を示す**。書いた説明ではなく計算した値を出す。

**この作りは検査を一つ失う。** 正解をモデルから導く以上、「正解がモデルと一致する」は
恒等式であり何も検査しない(HC-045)。測れるのは**設問の側**である:

- 正解が一意であること。**差の判定は相対と絶対の二本**で見る ——
  最良と次点の差が `最良の 15% 以上` かつ `0.03 以上`、または適格な型が 1 つだけ。
  絶対値だけで書くと**基準の値域に依存する**: 耐候の適性は 0〜1 に広く散るが、
  広さの適性は 0.14〜0.35 の狭い帯に収まるので、絶対 0.10 は「最良が次点の 1.4 倍」を
  要求してしまう(2026-09-08 実測)。**規則を変えたとき、それまで通っていた設問が
  すべて新しい規則でも通ることを確かめた** —— 一つを通すために緩めたのではない。
  実測(2026-09-07): 無風・無雨では全型とも適性 1.000 で差が付かず、**設問として不適格**。
  型を 5 種へ増やしたとき、この検査が二つの設問(強い雨・ポール 1 本)を落とした。
  条件を足して測り直した(強い雨に北 45% の風を、材料の設問をペグ 3 本と北東 55% に)。
- 「どれも適さない」が正解になる設問が 1 つ以上あること(**死んだ選択肢を置かない**)。
- **一度も正解にならない型があるなら、その型と理由を SPEC に書くこと。**
  実測(2026-09-08・居住性を足した後): **該当する型は無い**。
  居住性という第三の軸を足すまでは Lean-To と Diamond が該当していた(§4.12)。
  **軸を足すことが、この申告を空にする正しい直し方だった** ——
  設問をひねって無理に勝たせるのではなく、モデルが測っていないものを測るようにする。

**クイズはモデルの外を問えない。** 前が開いているか、設営の速さ、地面の状態は
適性に入っていないので、それを問う設問は書けない。

### 4.11 ロープワーク(F-21・原案 §15)

結び方は**実物のロープで確かめないと分からない**。だから画面が主張することを絞る。

- **手順は文で書く。図は模式図である**と画面に明記する。図はロープの通り道と、
  交差での上下だけを示す。写真のような正確さは主張しない。
- 各結び方は `data/knots.json` に持つ。ステップごとに `strands`(折れ線)と
  `crossings`(交差の座標・上を通るストランド・下を通るストランド)を持つ。
- **交差データは幾何で検算する**(G-28)。宣言した交差の座標が、宣言した 2 本の
  ストランドの実際の交点と一致しなければ落とす。手で書いた図のデータに対する非循環の検査である。
- **手順が進めば図も進むこと**(G-30)。連続するステップの図が同一なら落とす。
  文だけが進んで図が止まる欠陥は、交差の検算も viewBox の検査も要素数の検査も素通りする
  (loop_007 で実際に踏んだ)。
- **設営工程との対応は両方向で完全であること**(G-29)。ロープを扱う工程はどれかの結び方を指し、
  どの結び方も少なくとも一つの工程から参照される(死んだ項目を置かない)。

**出典の別を明示する。** FM 3-05.70 第 5 章が名指しする結び方は一つだけである ——
Poncho Lean-to の項の「Tie the ropes about waist high on the trees.
Use a round turn and two half hitches with a quick-release knot.」(2026-09-07 確認)。
原案 §15 が挙げる自在結び・もやい結びは**同章に記載が無い**ので、
「原案の候補・出典章に記載なし」と各項目に書く。

### 4.12 居住性(F-12・原案 §9.4)

耐風と排水だけでは、**前が大きく開く型や四方が均等な型の長所が測れない**。
loop_009 の実測で、鋤先が二軸でも材料でも他を上回るため Lean-To と Diamond が
どの設問でも勝てないことが分かった。そこで第三の軸を足す。

- **座高** `SIT_HEIGHT = 90 cm`。頭上がこれ以上ある所を「使える」とする。
- **頭上高**は稜線からの光線で出す。点 p から稜線(線分)への垂線の足 f を取り、
  f から p の向きへ光線を飛ばして屋根面の外へ出るまでの距離 `R` を求める。
  `頭上高 = h × (1 − |p − f| / R)`。h は立っているポールの最大高さ。
  **この式なら稜線が点に退化した錐(ティピー)も同じ扱いで測れる。**
- **使える床**は屋根面を 3 cm 刻みの格子で標本化し、頭上高が `SIT_HEIGHT` 以上の面積。
- **居住性** `habitability = 使える床 / 90,000 cm²`(タープ 300 cm 角の面積)。

**居住性は標本化した量である。** 格子は面積を刻み一列ぶんの粒度でしか数えられないので、
解析解が 0 になる場合(高さが座高ちょうどのとき、条件を満たすのは稜線の上だけ)でも
わずかな値が残る。0 と書かずに誤差として扱う(T-043)。

刻みは解析解と突き合わせて決めた。A-Frame の屋根面は矩形なので `R` が一定になり、
使える割合は閉形式で `1 − 90/120 = 0.25`、居住性は `54,000 × 0.25 / 90,000 = 0.15`。
3 cm 刻みでこれと一致する(T-042)。

実測(2026-09-08・理想の高さで):

| 型 | 居住性 | 耐風・排水での位置 |
|---|---:|---|
| Lean-To | 0.350 | 二軸では最下位級 |
| Diamond | 0.294 | 二軸では鋤先に劣る |
| 鋤先 | 0.153 | 二軸で最良 |
| A-Frame | 0.150 | 二軸で最良級 |
| ティピー | 0.144 | 排水は最良級 |

**軸を足したことで、二軸で負けていた二型が広さでは上位に来る。**
これは実際の選択とも合う —— Lean-To は前が開いて広く使え、A-Frame は狭いが強い。
その結果、クイズで一度も正解にならない型は無くなった(§4.10)。

## 5. データ(`data/shelters.json`・原案 §21)

| 属性 | 型 | 意味 |
|---|---|---|
| `id` `name` `nameJa` | string | 識別子・英名・和名 |
| `difficulty` | 1–4 | 原案 §12 の星数 |
| `field` | {w,h} | フィールド寸法(cm) |
| `tarp` | {folded,flat,pitched: [[x,y],…], panels: [{poly,shade}]} | §4.1 の三多角形と屋根面 |
| `ridge` | [[x,y],…] | 稜線(棟)の折れ線。端点は `panels` の頂点 |
| `opening` | number \| null | 開口が向く方位(度)。両側が閉じる型は `null`(§4.7)。幾何から決まる型は G-31 が検算する |
| `openingNote` | string(任意) | 開口が幾何から決まらない型で、その理由を書く欄 |
| `poles` | [{id,x,y,height:{min,ideal,max}}] | ポール目標 |
| `pegs` | [{id,x,y}] | ペグ目標 |
| `ropes` | [{id,anchor:{x,y},peg}] | ロープ(anchor は pitched 多角形の頂点または ポール位置) |
| `tolerance` | number | §4.2 の許容半径(cm) |
| `weather` | string[] | 向く天候(原案 §12「特徴」) |
| `notes` | string[] | 画面に出す設営の要点(和文) |
| `source` | {title,edition,year,figure,url,rights,usage} | F-13 の参考資料 |

原案 §21.2 の例示(A-Frame: poles 2 / pegs 6 / ropes 4)は例示であって規範ではない。
本 SPEC は**部品数を属性に持たず配列長から導く**(数を二重に持つと必ずずれる)。
A-Frame は 2 本のポール・6 本のペグ・6 本のロープ(棟の張り綱 2 + 四隅 4)とした。

## 6. 品質ゲート

| ID | ゲート | 検証 |
|---|---|---|
| G-01 | データ整合: ロープの `peg` 参照が実在し、全目標・全多角形頂点がフィールド内、ID が一意、ペグ目標同士が tolerance の 2 倍以上離れている(近いと判定が曖昧になる) | T-001 / T-002 |
| G-02 | 配置判定の陽性・陰性対照(許容内で充填 / 許容外で差し戻し+ミス / 充填済み目標は再充填しない) | T-003 / T-004 / T-005 |
| G-03 | 張力: 目標長で 100、差が広がるほど単調に下がる、±15 cm で 70、たるみ/届かないの文の向き | T-006 / T-007 / T-008 |
| G-04 | 完成述語: 正しい手順列で complete、六条件のそれぞれを一つだけ崩した変異体はすべて未完成 | T-009 / T-010 |
| G-05 | スコア: 0–1000 に収まる、ミス・秒・ヒントに対し単調非増加、ランク境界値 | T-011 / T-012 |
| G-06 | 保存: 往復で同値、壊れた値・例外を投げる storage でも落ちずに「記録なし」を返す、より低いスコアで上書きしない | T-013 / T-014 |
| G-07 | AUTO 手順列: データから生成した列を状態機械へ流すと complete に達する(三種すべて) | T-015 |
| G-08 | 字種: `data/shelters.json` の和文にキリル文字・ハングル・制御文字が無い(共通の text_hygiene は data/ を見ない) | T-016(陽性対照つき) |
| G-09 | 出荷ビルド(`tsc --noEmit && vite build`)が通る | B-01(検品は dist/ を配って行う) |
| G-10 | 実ブラウザ検品: Pointer 操作で A-Frame を完成させ、要素が viewBox 内、二幅で横溢れ無し | B-03 / B-04 / B-05 |
| G-11 | フッタ 5 項目・並び・固定 | B-06 |
| G-12 | 出典表示: 各シェルターの `source` 七項目が空でなく画面に出る | T-017(データ側)/ B-07(画面側) |
| G-13 | Go API とのスコア二実装照合 | 未実装(§7) |
| G-14 | 本番が手元と同じものを配っている(ビルド刻印)。刻印の材料は配信物を決める入力すべて | P-01 |
| G-15 | 本番で実際に動く(トップが 200・外部読込ゼロ・フッタ規約・AUTO が完成に達する) | P-02 / P-03 / P-04 / P-05 |
| G-16 | 正面投影幅が閉形式と一致(矩形で θ=0/45/90)。風速に対し荷重は二次、安定度は単調非増加 | T-020 / T-021 |
| G-17 | 向きが効くことの対照: A-Frame は棟に沿う風のほうが安定、Lean-To は開口を風下にすると安定、開口 null の型は向きで Cd が変わらない | T-022 |
| G-18 | 張力が効く: ロープがたるむと安定度が下がる。未接続なら hold=0 | T-023 |
| G-19 | 屋根面の水平距離と勾配がデータから一意に決まり、実測値と一致(A-Frame 90 cm / 53.1301 度など) | T-025 |
| G-20 | 排水度は勾配と張力に単調非減少で 0〜1。判定は雨量込みで、境界が SPEC どおり。雨量 0 では判定を出さない | T-026 / T-027 / T-029 |
| G-21 | 対照: 同じ勾配でも張力を落とすと溜まる。雨量が強いほど溜まる。最も緩い面が判定を決める | T-028 |
| G-22 | **各課題は型を選ばせる**: 達成できる型が 1 つ以上、できない型が 1 つ以上。実測の表と一致する | T-031 |
| G-23 | 達成判定は未達の理由を必ず列挙する。材料の上限・天候・時間・ミスのそれぞれが単独で未達にできる | T-032 |
| G-24 | クリア記録の往復。壊れた値・例外を投げる storage でも落ちない | T-033 |
| G-25 | 各設問の正解が一意(差 0.10 以上、または適格な型が 1 つ)。**陽性対照**: 差の付かない状況は不適格と判定される | T-035 |
| G-26 | 「どれも適さない」が正解になる設問が 1 つ以上ある(死んだ選択肢を置かない) | T-036 |
| G-27 | `data/quiz.json` の整合: ID 一意・状況が範囲内・字種に違反なし | T-037 |
| G-28 | 結び方の図の交差データを幾何で検算: 宣言した交差が、宣言した 2 本のストランドの実際の交点と一致し、上下が別のストランドを指す。全点が viewBox 内 | T-038 |
| G-29 | 設営工程と結び方の対応が両方向で完全(工程は結び方を指し、どの結び方も使われる)。出典の別が全項目に書かれている | T-039 |
| G-30 | **手順が進めば図も進む**: 連続するステップの図(ストランドと交差)が同一であってはならない。文だけが進んで図が止まると、画面が自分と矛盾する | T-040 |
| G-31 | 開口の向きが幾何と一致する: 片側を持ち上げる型では、開口は張った形の重心からポールの重心へ向かう方位と 45 度以内。対象外の型は理由を `openingNote` に書く | T-041 |
| G-32 | 居住性が閉形式と一致(A-Frame の矩形面で 1 − 90/120)。ポール高さに対し単調非減少で、座高未満では 0 | T-042 / T-043 |
| G-33 | 実測の順位表と一致し、**二軸で勝てない型が広さでは上位**であること(軸を足した目的が果たされている) | T-044 |

## 7. スコープ外・保留

- **Go Functions(F-16)は保留。** この機に Go ツールチェーンが無く、コンパイルも
  二実装照合(G-13)もできない。コンパイルしていない Go を `api/` に置くと Vercel のビルドが
  そこで落ちるので、置かない。原案 §20.2 が「クライアントだけで完全動作」を要求しているため、
  保留しても MVP の受入(原案 §28)は満たせる。
- 風・雨・MISSION・クイズ・ロープワーク(F-17・F-18)は Phase 2 以降。
- 地面の傾斜・昼夜・雪(原案 §35)はやらない。
- 実際の野外活動の安全性は保証しない(原案 §31)。画面の免責に載せる。

## 8. ループ計画

| ループ | 内容 | 状態 |
|---|---|---|
| L0 | 足場・SPEC・データ三種・純粋ロジック(判定・張力・完成・スコア・保存・AUTO 列)とテスト | 完了(2026-09-06) |
| L1 | SVG シミュレータ UI・ドラッグ・AUTO 再生・HINT・フッタ・出典表示・ビルド・実ブラウザ検品・README | 完了(2026-09-07) |
| L2 | Vercel 公開・app-menu 掲載・ビルド刻印つき本番検品 | 完了(2026-09-07) |
| L3 | 風(F-17) | 完了(2026-09-07) |
| L4 | 雨(F-19) | 完了(2026-09-07) |
| L5 | MISSION(F-18) | 完了(2026-09-07) |
| L6 | クイズ(F-20) | 完了(2026-09-07) |
| L7 | ロープワーク(F-21) | 完了(2026-09-07) |
| L8 | 図鑑の拡張(鋤先・ティピー) | 完了(2026-09-08) |
| L9+ | さらなる型の追加(原案 §35 は 20〜30 種) | 未着手 |
